require 'rails_helper'

RSpec.describe WaiversController do

  it '#new generates a waiver for the volunteer and region' do

  end

  context '#create waiver' do
    it 'redirects to new waiver url when waiver needs accepting' do
    end

    it 'redirects to root url when waiver is signed successfully' do
    end

    it 'redirects to new wavier url when an error occurs while signing' do
    end
  end

end
